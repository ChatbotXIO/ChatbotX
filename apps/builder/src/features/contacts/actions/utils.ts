import type { FilterContactSchema } from "@/features/contacts/filter/schema"

export const parseQueryFilterContact = (schema: FilterContactSchema) => {
  // todo parse condition contact
  return {}
}
