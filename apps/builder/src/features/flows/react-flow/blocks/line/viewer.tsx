import { Separator } from "@/components/ui/separator"
import type { LineBlockSchema } from "./schema"

export const LineBlockViewer = ({ data }: { data: LineBlockSchema }) => {
  return (
    <>
      <Separator className="my-2 w-full" />
    </>
  )
}
