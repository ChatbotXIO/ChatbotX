// Moved into `@chatbotx.io/flow-config/channel-rules` so the worker's import
// validation shares this map with `publishFlowAction`. Re-exported here so
// existing importers in the builder are unchanged.
export { channelAwareStepValidators } from "@chatbotx.io/flow-config"
